# Pokémon Fire Red Hack - 9 Gerações com Dificuldade Moderada

## 📋 Visão Geral

Esta é uma **programação completa em JavaScript** para criar uma hack ROM de Pokémon Fire Red com:

- ✅ **Dificuldade Moderada** - Balanceamento cuidadoso para desafio justo
- ✅ **Todas as 9 Gerações** - 1010+ Pokémon de 1998 a 2022
- ✅ **Mapa de Kanto** - Região original preservada
- ✅ **Pokémon Aleatórios** - Sistema de randomização inteligente

## 🚀 Como Usar

### Pré-requisitos
- Node.js instalado (versão 14+)
- ROM de Pokémon Fire Red (U) v1.0
- Pelo menos 50MB de espaço livre

### Instalação e Execução

```bash
# 1. Coloque sua ROM Fire Red na pasta
cp pokemon_fire_red.gba /workspace/

# 2. Execute o hack
node main.js pokemon_fire_red.gba

# 3. Ou com opções personalizadas
node main.js pokemon_fire_red.gba --output-name "minha_hack.gba"
```

## 📁 Estrutura dos Arquivos

```
/workspace/
├── main.js                    # Script principal de execução
├── pokemon_fire_red_hack.js   # Classe principal do hack
├── hack_systems.js            # Sistemas expandidos (tipos, habilidades, movimentos)
├── rom_utils.js               # Utilitários para manipulação de ROM
├── package.json               # Configurações do projeto
└── README.md                  # Este arquivo
```

## 🎮 Características Implementadas

### Sistema de Pokémon (9 Gerações)
- **Gen 1**: 001-151 (Kanto clássico)
- **Gen 2**: 152-251 (Johto + tipos Dark/Steel)
- **Gen 3**: 252-386 (Hoenn + sistema de habilidades)
- **Gen 4**: 387-493 (Sinnoh + divisão Physical/Special)
- **Gen 5**: 494-649 (Unova + habilidades ocultas)
- **Gen 6**: 650-721 (Kalos + tipo Fairy + Mega Evolução)
- **Gen 7**: 722-809 (Alola + Z-Moves + Ultra Beasts)
- **Gen 8**: 810-905 (Galar + Dynamax + Gigantamax)
- **Gen 9**: 906-1010 (Paldea + Terastalização)

### Sistema de Dificuldade Moderada
```javascript
// Níveis dos Líderes de Ginásio
brock: { baseLevel: 12, increment: 2 }      // 12-14
misty: { baseLevel: 18, increment: 2 }      // 18-20
surge: { baseLevel: 24, increment: 3 }      // 24-27
erika: { baseLevel: 29, increment: 3 }      // 29-32
koga: { baseLevel: 37, increment: 4 }       // 37-41
sabrina: { baseLevel: 43, increment: 4 }    // 43-47
blaine: { baseLevel: 47, increment: 5 }     // 47-52
giovanni: { baseLevel: 50, increment: 5 }   // 50-55

// Elite Four & Champion
lorelei: { baseLevel: 54, increment: 2 }    // 54-56
bruno: { baseLevel: 56, increment: 2 }      // 56-58
agatha: { baseLevel: 58, increment: 2 }     // 58-60
lance: { baseLevel: 60, increment: 3 }      // 60-63
champion: { baseLevel: 63, increment: 3 }   // 63-66
```

### Sistema de Randomização Inteligente
```javascript
const randomizationRules = {
    preserveLegendaries: true,        // Lendários mantêm raridade
    respectEvolutionStages: true,     // Respeita estágios evolutivos
    typeSimilarity: 0.3,             // 30% chance de tipos similares
    statSimilarity: 0.4,             // 40% chance de stats similares
    generationMix: true              // Mistura todas as gerações
};
```

### Sistema de Tipos Expandido (18 Tipos)
- Todos os tipos originais (Normal até Dragon)
- **Dark** e **Steel** (Gen 2)
- **Fairy** (Gen 6) com tabela de efetividade completa

### Movimentos Expandidos (860+)
- Movimentos de todas as 9 gerações
- **Physical/Special Split** implementado
- **Z-Moves**, **Max Moves** e **Tera Blast** incluídos
- Balanceamento de poder para dificuldade moderada

### Habilidades Expandidas (271)
- Habilidades das Gens 3-9
- **Mega Abilities**, **Hidden Abilities**
- Habilidades modernas como **Prankster**, **Technician**, **Adaptability**

## 🔧 Detalhes Técnicos

### Manipulação de ROM
```javascript
// Exemplo de como o código lê/escreve dados na ROM
static readUInt16LE(buffer, offset) {
    return buffer.readUInt16LE(offset);
}

static writeUInt16LE(buffer, value, offset) {
    buffer.writeUInt16LE(value, offset);
}
```

### Estrutura de Dados de Pokémon
```javascript
// Cada Pokémon tem 28 bytes de dados
static parsePokemonData(buffer, offset) {
    return {
        baseHP: buffer[offset],
        baseAttack: buffer[offset + 1],
        baseDefense: buffer[offset + 2],
        baseSpeed: buffer[offset + 3],
        baseSpAttack: buffer[offset + 4],
        baseSpDefense: buffer[offset + 5],
        type1: buffer[offset + 6],
        type2: buffer[offset + 7],
        catchRate: buffer[offset + 8],
        // ... mais campos
    };
}
```

### Algoritmo de Randomização
```javascript
generateAreaEncounters(area, rules) {
    const encounters = [];
    const availablePokemon = this.getAllPokemon();
    
    for (let i = 0; i < area.encounters; i++) {
        let selectedPokemon;
        
        if (rules.generationMix) {
            // Pesos por geração
            const genWeights = {
                gen1: 0.25, gen2: 0.15, gen3: 0.15,
                gen4: 0.12, gen5: 0.10, gen6: 0.08,
                gen7: 0.07, gen8: 0.05, gen9: 0.03
            };
            
            selectedPokemon = this.selectPokemonByWeight(availablePokemon, genWeights);
        }
        
        // Calcular nível e raridade
        const level = Math.floor(Math.random() * (area.level[1] - area.level[0] + 1)) + area.level[0];
        const rarity = this.calculateEncounterRate(i);
        
        encounters.push({
            pokemon: selectedPokemon,
            level: level,
            rate: rarity
        });
    }
    
    return encounters;
}
```

## 📊 Estatísticas da Hack

| Característica | Valor |
|----------------|-------|
| **Pokémon Total** | 1010+ |
| **Movimentos** | 860+ |
| **Habilidades** | 271 |
| **Tipos** | 18 |
| **Dificuldade** | Moderada (7/10) |
| **Regiões** | Kanto |
| **Randomização** | Inteligente |

## 🎯 Recursos Únicos

### 1. Balanceamento Inteligente
- Stats balanceados por geração
- Progressão suave de dificuldade
- IA melhorada para treinadores

### 2. Preservação da Experiência Original
- Mapa de Kanto intacto
- História original preservada
- Mecânicas familiares mantidas

### 3. Modernização Completa
- Todos os Pokémon até Gen 9
- Movimentos e habilidades atuais
- Sistema de tipos moderno

### 4. Sistema de Backup Automático
```javascript
static createBackup(romPath) {
    const backupPath = romPath.replace('.gba', '_backup.gba');
    fs.copyFileSync(romPath, backupPath);
    console.log(`Backup criado: ${backupPath}`);
    return backupPath;
}
```

## 🚨 Limitações Técnicas

1. **Sprites**: Pokémon das gerações mais recentes podem usar sprites placeholder
2. **Nomes**: Nomes muito longos podem ser truncados
3. **Compatibilidade**: Testado em emuladores padrão (VBA, mGBA)
4. **Tamanho**: ROM expandida pode ser maior que o original

## 🎮 Como Jogar

1. **Emulador Recomendado**: VisualBoy Advance, mGBA, ou similar
2. **Configurações**: Use configurações padrão do emulador
3. **Save**: Salve frequentemente devido às modificações
4. **Backup**: Mantenha sempre o backup da ROM original

## 🔄 Atualizações Futuras

- [ ] Sprites customizados para Gen 8-9
- [ ] Sistema de Mega Evolução visual
- [ ] Mais opções de randomização
- [ ] Interface gráfica para configuração
- [ ] Suporte a outras regiões

## 📝 Licença

Este projeto é fornecido apenas para fins educacionais. Pokémon é propriedade da Nintendo/Game Freak. Use apenas com ROMs que você possui legalmente.

---

**Desenvolvido com ❤️ usando JavaScript puro**

*Versão 1.0 - Todas as 9 gerações implementadas*